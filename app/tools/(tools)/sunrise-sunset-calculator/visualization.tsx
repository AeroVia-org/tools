"use client";

const InteractiveDefaultCalculator = () => {

  return (
    <div className="relative mx-auto w-full max-w-md">
      <h3 className="mb-2 text-center text-lg font-semibold text-gray-800 dark:text-gray-100">
        Interactive Default Calculator
      </h3>

      <p className="mt-1 text-center text-xs text-gray-500 dark:text-gray-400">
        Hover over the chart to see the result of adding one to a number.
      </p>
    </div>
  );
};

export default InteractiveDefaultCalculator;
